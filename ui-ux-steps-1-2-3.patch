diff --git a/src/_variables.scss b/src/_variables.scss
deleted file mode 100644
index e1a22a2..0000000
--- a/src/_variables.scss
+++ /dev/null
@@ -1,90 +0,0 @@
-// colors
-$primaryBg: #0b1020;
-
-$color-tennis: #d6ff41;
-$color-purple: #dabdff;
-$color-fire: #f87931;
-
-$color-gray-dark: #151b2d;
-$color-gray-caviar: #1a2235;
-$color-gray-medium: #232d45;
-$color-gray-jet: #34415d;
-$color-gray-light: #8490aa;
-
-$color-text: #f8fafc;
-$color-text-gray: #aeb8cb;
-$color-icy-lilac: #e9eaf8;
-$color-black: #000;
-
-$color-error: #ff6b78;
-$color-success: #8be28b;
-$color-blue: #5aa9ff;
-
-// semantic surfaces
-$surface-1: #11182a;
-$surface-2: #172139;
-$surface-3: #1e2a45;
-$color-border: rgba(174, 184, 203, 0.14);
-$color-border-strong: rgba(214, 255, 65, 0.35);
-$color-text-muted: #8490aa;
-$shadow-card: 0 18px 45px rgba(2, 6, 23, 0.24);
-$shadow-glow: 0 0 0 4px rgba(214, 255, 65, 0.1);
-
-// hover colors
-$hover-primary: #e5ff87;
-$hover-secondary: #f0e3ff;
-$hover-light: #c2c3d0;
-$hover-dark: #202b43;
-$hover-cancel: #f9935a;
-$hover-error: #e91000;
-
-// light shades success and hover
-$light-success: #b7dc99;
-$light-error: #e36b63;
-
-// gradients
-$gradient-bg: linear-gradient(160deg, #18233b 0%, #11182a 52%, #0d1425 100%);
-$gradient-horizontal: linear-gradient(90deg, #1d2943 0%, #d6ff41 50%, #1d2943 100%);
-$gradient-tennis: linear-gradient(90deg, #1d2943 0%, #d6ff41 50%, #1d2943 100%);
-
-// border-radius
-$radius-large: 24px;
-$radius-small: 16px;
-$radius-xs: 8px;
-
-$color-stroke: $color-border;
-$color-jet: $color-gray-jet;
-$color-white: white;
-
-// padding
-$padding-base: 2rem;
-$padding-retina: 1rem;
-
-// font sizes
-$font-2xs: 0.625rem;
-$font-xs: 0.75rem;
-$font-sm: 0.875rem;
-$font-base: 1rem;
-$font-md: 1.125rem;
-$font-lg: 1.5rem;
-$font-xl: 1.75rem;
-$font-xxl: 2rem;
-$font-3xl: 2.5rem;
-
-// letter spacing
-$spacing-small: 0.5px;
-$spacing: 1px;
-
-// sidebar width
-$sidebar-open-width: 15rem;
-$sidebar-closed-width: 4.5rem;
-
-$breakpoints: (
-  xxs: 790px,
-  xs: 883px,
-  s: 969px,
-  sm: 1030px,
-  md: 1085px,
-  lg: 1200px,
-  xl: 1283px,
-);
\ No newline at end of file
diff --git a/src/components/NavBar.jsx b/src/components/NavBar.jsx
index 647ae77..7d946b4 100644
--- a/src/components/NavBar.jsx
+++ b/src/components/NavBar.jsx
@@ -3,13 +3,22 @@ import styles from "../styles/layout/navbar.module.scss";
 import LanguageSelector from "./elements/LanguageSelector";
 import i18n from "../configuration/i18n";
 import MessageComponent from "../components/interface/MessageComponent";
+import Menu from "./icons/interface/Menu";
 
-const NavBar = ({ profiles }) => {
+const NavBar = ({ profiles, onMenuButtonClick }) => {
   const displayName = profiles?.data?.fullname || profiles?.firstname || "Guest";
   const initial = displayName.trim().charAt(0).toUpperCase() || "G";
 
   return (
     <div className={styles.navbarContainer}>
+      <button
+        type="button"
+        className={styles.menuButton}
+        onClick={onMenuButtonClick}
+        aria-label={i18n.t("sidebar.openMenu")}
+      >
+        <Menu />
+      </button>
       <div className={styles.userContainer}>
         <div className={styles.avatar} aria-hidden="true">{initial}</div>
         <div>
diff --git a/src/components/accounts/AccountComponent.jsx b/src/components/accounts/AccountComponent.jsx
index d182e42..9c58827 100644
--- a/src/components/accounts/AccountComponent.jsx
+++ b/src/components/accounts/AccountComponent.jsx
@@ -2,7 +2,8 @@ import React, { useEffect, useState } from "react";
 import Row from "./Row";
 import styles from "../../styles/components/accounts/accounts.module.scss";
 import AccountsInfo from "./AccountsInfo";
-import PlusButton from "../btns/PlusButton";
+import Button from "../btns/Button";
+import PlusIcon from "../icons/btns/PlusIcon";
 import CreateAccount from "../account/CreateAccount";
 import SearchInput from "../inputs/SearchInput";
 import i18n from "../../configuration/i18n";
@@ -44,9 +45,11 @@ const AccountComponent = ({ accounts }) => {
       <div className={styles.divider}></div>
       <div className={styles.accountHeader}>
         <div>
-          <PlusButton
+          <Button
             text={i18n.t("accountsPage.buttonText")}
             type="button"
+            variant="primary"
+            Icon={PlusIcon}
             onClick={() => setOpenCreateAccount(true)}
           />
         </div>
diff --git a/src/components/btns/ActivateButton.js b/src/components/btns/ActivateButton.js
deleted file mode 100644
index 183f251..0000000
--- a/src/components/btns/ActivateButton.js
+++ /dev/null
@@ -1,29 +0,0 @@
-import styles from "src/styles/elements/btns/activateButton.module.scss";
-
-export default function ActivateButton({
-  initialText = "Activate",
-  loadingText = "Waiting",
-  completedText = "Activated!",
-  onClick,
-  buttonState,
-  textState,
-}) {
-  return (
-    <button
-      className={`${styles.activateBtn} ${styles[buttonState]} ${styles[textState]}`}
-      onClick={onClick}
-    >
-      <span className={styles.circle}>
-        <span className={`${styles.icon} ${styles.arrow}`}>➜</span>
-        <span className={`${styles.icon} ${styles.check}`}>✔︎</span>
-      </span>
-      <span className={styles.text}>
-        {buttonState === "loading"
-          ? loadingText
-          : buttonState === "activated"
-          ? completedText
-          : initialText}
-      </span>
-    </button>
-  );
-}
diff --git a/src/components/btns/ArrowButton.js b/src/components/btns/ArrowButton.js
deleted file mode 100644
index 2b20106..0000000
--- a/src/components/btns/ArrowButton.js
+++ /dev/null
@@ -1,60 +0,0 @@
-import styles from "src/styles/elements/btns/arrowButton.module.scss";
-import PropTypes from "prop-types";
-import ArrowBtnIcon from "src/components/icons/btns/ArrowBtnIcon";
-
-export default function ArrowButton({
-  text,
-  variant,
-  onClick,
-  disabled = false,
-  fontSize,
-  borderSize,
-  fontWeight,
-}) {
-  const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
-
-  // conditional expression that creates a style object based on whether
-  // the fontSize prop is provided or not
-  // if not provided it will use the font size that is set in the scss file
-  const buttonStyle = {
-    ...(fontSize ? { fontSize } : {}),
-    ...(borderSize ? { borderWidth: borderSize } : {}),
-    ...(fontWeight ? { fontWeight } : {}),
-  };
-
-  // calculate the icon size based on font size (if provided)
-  // parse the fontSize value and convert to a number for icon sizing
-  let iconSize;
-  if (fontSize) {
-    const sizeValue = parseFloat(fontSize);
-    if (!isNaN(sizeValue)) {
-      iconSize = Math.max(7, sizeValue * 0.6);
-    }
-  }
-
-  return (
-    <button
-      className={classNames}
-      onClick={onClick}
-      disabled={disabled}
-      style={buttonStyle}
-    >
-      {text}
-      <ArrowBtnIcon
-        className={styles.arrowIcon}
-        style={iconSize}
-        width={iconSize}
-        height={iconSize}
-      />
-    </button>
-  );
-}
-
-ArrowButton.propTypes = {
-  text: PropTypes.string.isRequired,
-  variant: PropTypes.oneOf(["primary", "secondary", "cancel", "light", "dark"])
-    .isRequired,
-  onClick: PropTypes.func,
-  disabled: PropTypes.bool,
-  fontSize: PropTypes.string,
-};
diff --git a/src/components/btns/Button.jsx b/src/components/btns/Button.jsx
index 2340c34..76d4ce5 100644
--- a/src/components/btns/Button.jsx
+++ b/src/components/btns/Button.jsx
@@ -10,6 +10,9 @@ export default function Button({
   paddingSize,
   borderSize,
   type = "button",
+  Icon,
+  iconPosition = "start",
+  iconProps = {},
 }) {
   const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
 
@@ -19,6 +22,23 @@ export default function Button({
     ...(paddingSize ? { padding: paddingSize } : {}),
   };
 
+  // icon size follows the button's font size so it always matches, unless
+  // the caller explicitly overrides it via iconProps.
+  let iconSize = iconProps.width;
+  if (!iconSize && fontSize) {
+    const sizeValue = parseFloat(fontSize);
+    if (!isNaN(sizeValue)) iconSize = Math.max(7, sizeValue * 0.6);
+  }
+
+  const icon = Icon ? (
+    <Icon
+      className={styles.icon}
+      width={iconSize}
+      height={iconSize}
+      {...iconProps}
+    />
+  ) : null;
+
   return (
     <button
       type={type}
@@ -27,7 +47,9 @@ export default function Button({
       disabled={disabled}
       style={buttonStyle}
     >
+      {iconPosition === "start" && icon}
       {text}
+      {iconPosition === "end" && icon}
     </button>
   );
 }
@@ -52,4 +74,7 @@ Button.propTypes = {
   ]).isRequired,
   onClick: PropTypes.func,
   disabled: PropTypes.bool,
+  Icon: PropTypes.elementType,
+  iconPosition: PropTypes.oneOf(["start", "end"]),
+  iconProps: PropTypes.object,
 };
diff --git a/src/components/btns/EditButton.jsx b/src/components/btns/EditButton.jsx
deleted file mode 100644
index 62ba7ba..0000000
--- a/src/components/btns/EditButton.jsx
+++ /dev/null
@@ -1,55 +0,0 @@
-import styles from "src/styles/elements/btns/editButton.module.scss";
-import PropTypes from "prop-types";
-import EditIcon from "../icons/btns/EditIcon";
-
-export default function EditButton({
-  text,
-  variant,
-  onClick,
-  disabled = false,
-  fontSize,
-  borderSize,
-  backgroundColor,
-  fontWeight,
-}) {
-  const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
-
-  const buttonStyle = {
-    ...(fontSize ? { fontSize } : {}),
-    ...(borderSize ? { borderWidth: borderSize } : {}),
-    ...(backgroundColor === "black" ? { backgroundColor: "black" } : {}),
-    ...(fontWeight ? { fontWeight } : {}),
-  };
-
-  let iconSize;
-  if (fontSize) {
-    const sizeValue = parseFloat(fontSize);
-    if (!isNaN(sizeValue)) {
-      iconSize = Math.max(7, sizeValue * 0.6);
-    }
-  }
-
-  return (
-    <button
-      className={classNames}
-      onClick={onClick}
-      disabled={disabled}
-      style={buttonStyle}
-    >
-      <EditIcon
-        className={styles.edit}
-        style={iconSize}
-        width={iconSize}
-        height={iconSize}
-      />
-      {text}
-    </button>
-  );
-}
-
-EditButton.propTypes = {
-  text: PropTypes.string.isRequired,
-  variant: PropTypes.oneOf(["primary", "secondary", "light"]),
-  onClick: PropTypes.func,
-  disabled: PropTypes.bool,
-};
diff --git a/src/components/btns/IconButton.js b/src/components/btns/IconButton.js
deleted file mode 100644
index 72ba1be..0000000
--- a/src/components/btns/IconButton.js
+++ /dev/null
@@ -1,62 +0,0 @@
-import styles from "src/styles/elements/btns/iconButton.module.scss";
-import PropTypes from "prop-types";
-
-export default function IconButton({
-  text,
-  variant,
-  onClick,
-  disabled = false,
-  fontSize,
-  borderSize,
-  Icon,
-  iconProps = {},
-  backgroundColor,
-  fontWeight,
-}) {
-  const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
-
-  const buttonStyle = {
-    ...(fontSize ? { fontSize } : {}),
-    ...(borderSize ? { borderWidth: borderSize } : {}),
-    ...(backgroundColor === "black" ? { backgroundColor: "black" } : {}),
-    ...(fontWeight ? { fontWeight } : {}),
-  };
-
-  let iconSize;
-  if (fontSize) {
-    const sizeValue = parseFloat(fontSize);
-    if (!isNaN(sizeValue)) {
-      iconSize = Math.max(7, sizeValue * 0.6);
-    }
-  }
-
-  return (
-    <button
-      className={classNames}
-      onClick={onClick}
-      disabled={disabled}
-      style={buttonStyle}
-    >
-      {Icon && (
-        <Icon
-          className={styles.icon}
-          width={iconSize}
-          height={iconSize}
-          {...iconProps}
-        />
-      )}
-      {text}
-    </button>
-  );
-}
-
-IconButton.propTypes = {
-  text: PropTypes.string.isRequired,
-  variant: PropTypes.oneOf(["primary", "secondary", "cancel", "light"])
-    .isRequired,
-  onClick: PropTypes.func,
-  disabled: PropTypes.bool,
-  fontSize: PropTypes.string,
-  Icon: PropTypes.elementType,
-  iconProps: PropTypes.object,
-};
diff --git a/src/components/btns/PlusButton.jsx b/src/components/btns/PlusButton.jsx
deleted file mode 100644
index ef7c3d9..0000000
--- a/src/components/btns/PlusButton.jsx
+++ /dev/null
@@ -1,50 +0,0 @@
-import styles from "../../styles/btns/plusButton.module.scss";
-import PlusIcon from "../../components/icons/btns/PlusIcon";
-import PropTypes from "prop-types";
-
-export default function PlusButton({
-  text,
-  variant,
-  onClick,
-  disabled = false,
-  fontSize,
-  borderSize,
-  backgroundColor,
-  fontWeight,
-}) {
-  const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
-
-  const buttonStyle = {
-    ...(fontSize ? { fontSize } : {}),
-    ...(borderSize ? { borderWidth: borderSize } : {}),
-    ...(backgroundColor === "black" ? { backgroundColor: "black" } : {}),
-    ...(fontWeight ? { fontWeight } : {}),
-  };
-
-  let iconSize;
-  if (fontSize) {
-    const sizeValue = parseFloat(fontSize);
-    if (!isNaN(sizeValue)) {
-      iconSize = Math.max(7, sizeValue * 0.6);
-    }
-  }
-
-  return (
-    <button
-      style={buttonStyle}
-      className={classNames}
-      onClick={onClick}
-      disabled={disabled}
-    >
-      <PlusIcon style={iconSize} width={iconSize} height={iconSize} />
-      {text}
-    </button>
-  );
-}
-
-PlusButton.propTypes = {
-  text: PropTypes.string.isRequired,
-  variant: PropTypes.oneOf(["primary", "secondary", "light"]).isRequired,
-  onClick: PropTypes.func,
-  disabled: PropTypes.bool,
-};
diff --git a/src/components/btns/RequestLinkButton.js b/src/components/btns/RequestLinkButton.js
deleted file mode 100644
index dff5d6b..0000000
--- a/src/components/btns/RequestLinkButton.js
+++ /dev/null
@@ -1,29 +0,0 @@
-import styles from "src/styles/elements/btns/requestLinkButton.module.scss";
-
-export default function RequestLinkButton({
-  initialText = "Request new link",
-  loadingText = "Waiting",
-  completedText = "Requested!",
-  onClick,
-  buttonState,
-  textState,
-}) {
-  return (
-    <button
-      className={`${styles.newLinkBtn} ${styles[buttonState]} ${styles[textState]}`}
-      onClick={onClick}
-    >
-      <span className={styles.circle}>
-        <span className={`${styles.icon} ${styles.arrow}`}>➜</span>
-        <span className={`${styles.icon} ${styles.check}`}>✔︎</span>
-      </span>
-      <span className={styles.text}>
-        {buttonState === "loading"
-          ? loadingText
-          : buttonState === "activated"
-          ? completedText
-          : initialText}
-      </span>
-    </button>
-  );
-}
diff --git a/src/components/btns/SquareButton.js b/src/components/btns/SquareButton.js
deleted file mode 100644
index e5198a4..0000000
--- a/src/components/btns/SquareButton.js
+++ /dev/null
@@ -1,43 +0,0 @@
-import styles from "src/styles/elements/btns/squareButton.module.scss";
-import PropTypes from "prop-types";
-
-export default function SquareButton({
-  text,
-  variant,
-  onClick,
-  disabled = false,
-  fontSize,
-  paddingSize,
-  borderSize,
-  backgroundColor,
-  fontWeight,
-}) {
-  const classNames = `${styles.btn} ${styles[variant] || styles.primary}`;
-
-  const buttonStyle = {
-    ...(fontSize ? { fontSize } : {}),
-    ...(borderSize ? { borderWidth: borderSize } : {}),
-    ...(paddingSize ? { padding: paddingSize } : {}),
-    ...(backgroundColor === "black" ? { backgroundColor: "black" } : {}),
-    ...(fontWeight ? { fontWeight } : {}),
-  };
-
-  return (
-    <button
-      className={classNames}
-      onClick={onClick}
-      disabled={disabled}
-      style={buttonStyle}
-    >
-      {text}
-    </button>
-  );
-}
-
-SquareButton.propTypes = {
-  text: PropTypes.string.isRequired,
-  variant: PropTypes.oneOf(["primary", "secondary", "cancel", "light"])
-    .isRequired,
-  onClick: PropTypes.func,
-  disabled: PropTypes.bool,
-};
diff --git a/src/components/icons/interface/Close.js b/src/components/icons/interface/Close.jsx
similarity index 100%
rename from src/components/icons/interface/Close.js
rename to src/components/icons/interface/Close.jsx
diff --git a/src/components/icons/interface/Menu.jsx b/src/components/icons/interface/Menu.jsx
new file mode 100644
index 0000000..52ee26d
--- /dev/null
+++ b/src/components/icons/interface/Menu.jsx
@@ -0,0 +1,25 @@
+import * as React from "react";
+const Menu = ({ width = 18, height = 14, className, color = "currentColor" }) => (
+  <svg
+    width={width}
+    height={height}
+    viewBox="0 0 18 14"
+    fill="none"
+    xmlns="http://www.w3.org/2000/svg"
+    className={className}
+  >
+    <path
+      d="M0 1C0 0.447715 0.447715 0 1 0H17C17.5523 0 18 0.447715 18 1C18 1.55228 17.5523 2 17 2H1C0.447715 2 0 1.55228 0 1Z"
+      fill={color}
+    />
+    <path
+      d="M0 7C0 6.44772 0.447715 6 1 6H17C17.5523 6 18 6.44772 18 7C18 7.55228 17.5523 8 17 8H1C0.447715 8 0 7.55228 0 7Z"
+      fill={color}
+    />
+    <path
+      d="M1 12C0.447715 12 0 12.4477 0 13C0 13.5523 0.447715 14 1 14H17C17.5523 14 18 13.5523 18 13C18 12.4477 17.5523 12 17 12H1Z"
+      fill={color}
+    />
+  </svg>
+);
+export default Menu;
diff --git a/src/components/sidebar/MenuItem.jsx b/src/components/sidebar/MenuItem.jsx
index b771c35..bc0198b 100644
--- a/src/components/sidebar/MenuItem.jsx
+++ b/src/components/sidebar/MenuItem.jsx
@@ -2,7 +2,7 @@ import React from "react";
 import { Link, useLocation } from "react-router-dom";
 import styles from "../../styles/layout/sidebar/menuItem.module.scss";
 
-export default function MenuItem({ item, isOpen }) {
+export default function MenuItem({ item, isOpen, onClick }) {
   const location = useLocation();
   const isActive = location.pathname === item.link;
   const menuItemClasses = [
@@ -13,7 +13,7 @@ export default function MenuItem({ item, isOpen }) {
 
   return (
     <li className={menuItemClasses}>
-      <Link to={item.link} className={styles.linkWrapper}>
+      <Link to={item.link} className={styles.linkWrapper} onClick={onClick}>
         <div className={styles.item}>
           <div className={styles.menuIcon}>
             {item.icon
diff --git a/src/components/sidebar/Sidebar.jsx b/src/components/sidebar/Sidebar.jsx
index 65eb88f..0a80cdb 100644
--- a/src/components/sidebar/Sidebar.jsx
+++ b/src/components/sidebar/Sidebar.jsx
@@ -3,13 +3,14 @@ import { useNavigate } from "react-router-dom";
 import { useAuth } from "../../contexts/AuthContext.jsx";
 import styles from "../../styles/layout/sidebar/sidebar.module.scss";
 import LogoutIcon from "../icons/interface/Logout";
+import Close from "../icons/interface/Close";
 import MenuItem from "./MenuItem";
 import SidebarTop from "./SidebarTop";
 import DoubleChevronLeft from "../icons/chevrons/DoubleChevronLeft.jsx";
 import DoubleChevronRight from "../icons/chevrons/DoubleChevronRight.jsx";
 import i18n from "../../configuration/i18n.js";
 
-export default function Sidebar({ menuItems }) {
+export default function Sidebar({ menuItems, isMobileOpen = false, onMobileClose }) {
   const navigate = useNavigate();
   const { logout } = useAuth();
   const [isOpen, setIsOpen] = useState(window.innerWidth >= 1024);
@@ -21,43 +22,78 @@ export default function Sidebar({ menuItems }) {
     return () => window.removeEventListener("resize", handleResize);
   }, []);
 
+  // امنع سكرول الصفحة اللي وراء الـ drawer وهو مفتوح على الموبايل
+  useEffect(() => {
+    if (isMobileOpen) {
+      document.body.style.overflow = "hidden";
+    } else {
+      document.body.style.overflow = "";
+    }
+    return () => { document.body.style.overflow = ""; };
+  }, [isMobileOpen]);
+
   const handleMenuItemClick = (index, link) => {
     setActiveItem(index);
     if (link) navigate(link);
+    onMobileClose?.();
   };
 
   const handleLogout = async () => {
     try {
       await logout();
       navigate("/auth/login");
+      onMobileClose?.();
     } catch (err) { console.error(i18n.t("logoutError"), err); }
   };
 
+  // على الموبايل الـ drawer دايماً بيبان كامل (أيقونة + نص)، بغض النظر
+  // عن حالة الطي المستقلة المستخدمة على الديسكتوب (isOpen).
+  const showFullContent = isOpen || isMobileOpen;
+
   return (
-    <div className={`${styles.sidebarContainer} ${isOpen ? styles.open : styles.closed}`}>
-      <div className={styles.top}><SidebarTop isOpen={isOpen} /></div>
-      
-      <div className={styles.btn} onClick={() => setIsOpen(!isOpen)}>
-        {isOpen ? <DoubleChevronLeft  /> : <DoubleChevronRight />}
-      </div>
-      <div className={styles.menuContainer}>
-      <ul className={styles.menu}>
-        {menuItems.map((item, index) => (
-          <MenuItem
-            key={index}
-            item={item}
-            isOpen={isOpen}
-            isActive={activeItem === index}
-            onClick={() => handleMenuItemClick(index, item.link)}
-          />
-        ))}
-      </ul>
-      </div>
+    <>
+      <div
+        className={`${styles.backdrop} ${isMobileOpen ? styles.visible : ""}`}
+        onClick={onMobileClose}
+        aria-hidden="true"
+      />
+      <div
+        className={`${styles.sidebarContainer} ${showFullContent ? styles.open : styles.closed} ${isMobileOpen ? styles.mobileOpen : ""}`}
+      >
+        <div className={styles.top}><SidebarTop isOpen={showFullContent} /></div>
+
+        <div className={styles.btn} onClick={() => setIsOpen(!isOpen)}>
+          {isOpen ? <DoubleChevronLeft  /> : <DoubleChevronRight />}
+        </div>
+
+        <button
+          type="button"
+          className={styles.mobileClose}
+          onClick={onMobileClose}
+          aria-label={i18n.t("sidebar.closeMenu")}
+        >
+          <Close />
+        </button>
+
+        <div className={styles.menuContainer}>
+        <ul className={styles.menu}>
+          {menuItems.map((item, index) => (
+            <MenuItem
+              key={index}
+              item={item}
+              isOpen={showFullContent}
+              isActive={activeItem === index}
+              onClick={() => handleMenuItemClick(index, item.link)}
+            />
+          ))}
+        </ul>
+        </div>
 
-      <div className={styles.logout} onClick={handleLogout}>
-        <LogoutIcon />
-        {isOpen && <p>{i18n.t("sidebar.logout")}</p>}
+        <div className={styles.logout} onClick={handleLogout}>
+          <LogoutIcon />
+          {showFullContent && <p>{i18n.t("sidebar.logout")}</p>}
+        </div>
       </div>
-    </div>
+    </>
   );
 }
\ No newline at end of file
diff --git a/src/components/user/UserLyout.jsx b/src/components/user/UserLyout.jsx
index 61ec4c2..a1556da 100644
--- a/src/components/user/UserLyout.jsx
+++ b/src/components/user/UserLyout.jsx
@@ -8,6 +8,7 @@ import styles from "../../styles/layout/layout.module.scss";
 
 const UserLayout = () => {
   const [menuItems, setMenuItems] = useState(getUserMenuItems());
+  const [isMobileNavOpen, setIsMobileNavOpen] = useState(false);
   const [profile, setProfile] = useState(() => {
     try {
       return JSON.parse(localStorage.getItem("profiles")) || null;
@@ -37,12 +38,19 @@ const UserLayout = () => {
   return (
     <div className={styles.layout}>
       <aside className={styles.sidebar}>
-        <Sidebar menuItems={menuItems} />
+        <Sidebar
+          menuItems={menuItems}
+          isMobileOpen={isMobileNavOpen}
+          onMobileClose={() => setIsMobileNavOpen(false)}
+        />
       </aside>
       <div className={styles.bodyLayout}>
         <div className={styles.content}>
           <header className={styles.topContent}>
-            <NavBar profiles={profile} />
+            <NavBar
+              profiles={profile}
+              onMenuButtonClick={() => setIsMobileNavOpen(true)}
+            />
           </header>
           <main className={styles.mainContent}>
             <Outlet />
diff --git a/src/index.scss b/src/index.scss
index c9c2869..cb5ca0f 100644
--- a/src/index.scss
+++ b/src/index.scss
@@ -1,4 +1,4 @@
-@use "./variables" as variable;
+@use "./styles/variables" as variable;
 @import url("https://fonts.googleapis.com/css2?family=Darker+Grotesque:wght@300..900&family=Special+Gothic+Expanded+One&display=swap");
 
 * {
diff --git a/src/locales/ar/translation.json b/src/locales/ar/translation.json
index 1c94683..662bd7d 100644
--- a/src/locales/ar/translation.json
+++ b/src/locales/ar/translation.json
@@ -11,7 +11,9 @@
     "settings": "الإعدادات"
   },
   "sidebar": {
-    "logout": "تسجيل الخروج"
+    "logout": "تسجيل الخروج",
+    "openMenu": "فتح القائمة",
+    "closeMenu": "إغلاق القائمة"
   },
   "forgotPassword": {
     "header": "نسيت كلمة السر",
diff --git a/src/locales/en/translation.json b/src/locales/en/translation.json
index 62fdaf0..06e48d8 100644
--- a/src/locales/en/translation.json
+++ b/src/locales/en/translation.json
@@ -11,7 +11,9 @@
     "settings": "Settings"
   },
   "sidebar": {
-    "logout": "Logout"
+    "logout": "Logout",
+    "openMenu": "Open menu",
+    "closeMenu": "Close menu"
   },
   "forgotPassword": {
     "header": "Reset your password",
diff --git a/src/locales/sv/translation.json b/src/locales/sv/translation.json
index 78e0533..f3bc228 100644
--- a/src/locales/sv/translation.json
+++ b/src/locales/sv/translation.json
@@ -11,7 +11,9 @@
     "settings": "Inställningar"
   },
   "sidebar": {
-    "logout": "Logga ut"
+    "logout": "Logga ut",
+    "openMenu": "Öppna meny",
+    "closeMenu": "Stäng meny"
   },
   "forgotPassword": {
     "header": "Återställ ditt lösenord",
diff --git a/src/styles/_variables.scss b/src/styles/_variables.scss
index ca69de5..e10077b 100644
--- a/src/styles/_variables.scss
+++ b/src/styles/_variables.scss
@@ -1,3 +1,5 @@
+@use "sass:map";
+
 // colors
 $primaryBg: #0b1020;
 
@@ -80,12 +82,22 @@ $spacing: 1px;
 $sidebar-open-width: 15rem;
 $sidebar-closed-width: 4.5rem;
 
+// breakpoints
+// تطابق النقاط الحقيقية المستخدمة فعلياً بالتطبيق (auth forms, navbar,
+// account rows, layout/sidebar, dashboard) بدل أرقام كانت غير مستخدمة أصلاً.
 $breakpoints: (
-  xxs: 790px,
-  xs: 883px,
-  s: 969px,
-  sm: 1030px,
-  md: 1085px,
-  lg: 1200px,
-  xl: 1283px,
-);
\ No newline at end of file
+  mobile: 520px,       // auth forms, navbar
+  mobile-lg: 620px,    // account rows
+  tablet: 768px,       // layout, sidebar
+  tablet-lg: 980px,    // dashboard
+);
+
+@mixin respond($key) {
+  @if map.has-key($breakpoints, $key) {
+    @media (max-width: map.get($breakpoints, $key)) {
+      @content;
+    }
+  } @else {
+    @warn "Unknown breakpoint: #{$key}. Available: #{map.keys($breakpoints)}";
+  }
+}
\ No newline at end of file
diff --git a/src/styles/auth/forgotPassword.module.scss b/src/styles/auth/forgotPassword.module.scss
index 34756d9..3d79cb0 100644
--- a/src/styles/auth/forgotPassword.module.scss
+++ b/src/styles/auth/forgotPassword.module.scss
@@ -89,7 +89,7 @@
   border-radius: 0.75rem;
 }
 
-@media (max-width: 520px) {
+@include variable.respond(mobile) {
   .authLayout {
     padding: 0.75rem;
   }
diff --git a/src/styles/auth/login.module.scss b/src/styles/auth/login.module.scss
index eee3276..1e32b71 100644
--- a/src/styles/auth/login.module.scss
+++ b/src/styles/auth/login.module.scss
@@ -111,7 +111,7 @@
   font-size: variable.$font-sm;
 }
 
-@media (max-width: 520px) {
+@include variable.respond(mobile) {
   .authLayout {
     padding: 0.75rem;
   }
diff --git a/src/styles/auth/loginTerminal.module.scss b/src/styles/auth/loginTerminal.module.scss
index 48ad0ce..6d250df 100644
--- a/src/styles/auth/loginTerminal.module.scss
+++ b/src/styles/auth/loginTerminal.module.scss
@@ -69,7 +69,7 @@
   50% { border-color: transparent; }
 }
 
-@media (max-width: 520px) {
+@include variable.respond(mobile) {
   .terminal {
     min-height: 8.5rem;
   }
diff --git a/src/styles/btns/activateButton.module.scss b/src/styles/btns/activateButton.module.scss
deleted file mode 100644
index 189cecc..0000000
--- a/src/styles/btns/activateButton.module.scss
+++ /dev/null
@@ -1,118 +0,0 @@
-@use "../../variables" as variable;
-
-.activateBtn {
-  margin: 2.875rem 1.875rem; // was 46px 30px;
-  display: flex;
-  align-items: center;
-  gap: 1rem;
-  padding: 0.625rem 1.563rem; // was 10px 25px;
-  background: none;
-  color: variable.$color-purple;
-  border: 2px solid variable.$color-purple;
-  border-radius: 100px;
-  cursor: pointer;
-  transition: background-color 0.3s ease, color 0.3s ease;
-  font-size: variable.$font-base;
-  font-weight: 500;
-  letter-spacing: variable.$spacing;
-  text-align: center;
-
-  .circle {
-    width: 19px;
-    height: 19px;
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    border: 2px solid variable.$color-purple;
-    background-color: variable.$color-purple;
-    border-radius: 50%;
-    position: relative;
-    transition: border-color 0.3s ease;
-
-    .icon {
-      position: absolute;
-      font-size: variable.$font-xs;
-      opacity: 0;
-      transition: opacity 0.3s ease;
-      font-weight: bold;
-    }
-
-    .arrow {
-      opacity: 1;
-
-      color: variable.$color-black;
-    }
-
-    .check {
-      opacity: 0;
-      color: variable.$color-black;
-    }
-  }
-
-  .text {
-    transition: color 0.3s ease;
-  }
-
-  &:hover:not(.loading):not(.activated) {
-    .circle .arrow {
-      opacity: 0;
-      transform: scale(0.8) rotate(45deg);
-    }
-
-    .circle .check {
-      opacity: 1;
-      transform: scale(1) rotate(0deg);
-    }
-  }
-
-  &.loading {
-    border: 1px solid variable.$color-tennis;
-
-    .circle {
-      border: 2px solid transparent;
-      border-top: 2px solid variable.$color-tennis;
-      animation: spin 1s linear infinite;
-      background-color: transparent;
-
-      .icon {
-        opacity: 0;
-      }
-    }
-
-    .text {
-      color: variable.$color-tennis;
-    }
-  }
-
-  &.activated {
-    background-color: variable.$color-black;
-    color: variable.$color-tennis;
-    border: 1px solid variable.$color-tennis;
-
-    .circle {
-      border-color: variable.$color-tennis;
-      background-color: variable.$color-tennis;
-
-      .arrow {
-        opacity: 0;
-      }
-
-      .check {
-        opacity: 1;
-      }
-    }
-
-    .text {
-      color: variable.$color-tennis;
-    }
-  }
-}
-
-@keyframes spin {
-  0% {
-    transform: rotate(0deg);
-  }
-  100% {
-    transform: rotate(360deg);
-  }
-}
diff --git a/src/styles/btns/arrowButton.module.scss b/src/styles/btns/arrowButton.module.scss
deleted file mode 100644
index 1f350d7..0000000
--- a/src/styles/btns/arrowButton.module.scss
+++ /dev/null
@@ -1,125 +0,0 @@
-@use "../../variables" as variable;
-
-.btn {
-  font-size: variable.$font-xs;
-  border-radius: 7px;
-  font-weight: 600;
-  cursor: pointer;
-  letter-spacing: 0.3px;
-  transition: all 0.5s ease-in-out;
-  display: inline-flex;
-  align-items: center;
-  gap: 7px;
-  flex-grow: 0;
-  align-self: flex-start;
-  padding: 5px 10px;
-
-  svg {
-    transition: all 0.5s ease-in-out;
-  }
-}
-
-.primary {
-  background-color: variable.$primaryBg;
-  color: variable.$color-tennis;
-  border: 1px solid variable.$color-tennis;
-
-  .arrowIcon {
-    fill: variable.$color-tennis;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.secondary {
-  background-color: variable.$color-black;
-  color: variable.$color-purple;
-  border: 1px solid variable.$color-purple;
-
-  .arrowIcon {
-    fill: variable.$color-purple;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    background-color: variable.$color-text;
-  }
-}
-
-.light {
-  background-color: variable.$color-icy-lilac;
-  color: variable.$color-black;
-  border: 1px solid variable.$color-black;
-
-  .arrowIcon {
-    fill: variable.$color-black;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    background-color: variable.$color-text-gray;
-  }
-}
-
-.dark {
-  background-color: variable.$primaryBg;
-  color: variable.$color-text;
-  border: 1px solid variable.$color-text;
-
-  .arrowIcon {
-    fill: variable.$color-text;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text-gray;
-    border: 1px solid variable.$color-text-gray;
-
-    .arrowIcon {
-      fill: variable.$color-text-gray;
-    }
-  }
-}
-
-@media only screen and (-webkit-min-device-pixel-ratio: 2),
-  only screen and (min--moz-device-pixel-ratio: 2),
-  only screen and (-o-min-device-pixel-ratio: 2/1),
-  only screen and (min-device-pixel-ratio: 2),
-  only screen and (min-resolution: 192dpi),
-  only screen and (min-resolution: 2dppx) {
-  .btn {
-    border-radius: 5px;
-    letter-spacing: 0.5px;
-  }
-
-  .white {
-    font-size: variable.$font-2xs;
-    background-color: variable.$color-text;
-    color: variable.$color-gray-dark;
-    order: 1px solid variable.$color-gray-dark;
-    padding: 5px;
-
-    .arrowIcon {
-      fill: variable.$color-gray-dark;
-      margin-left: 7px;
-      margin-top: 1px;
-      height: 8px;
-    }
-  }
-
-  .black {
-    padding: 4px 10px;
-  }
-}
diff --git a/src/styles/btns/button.module.scss b/src/styles/btns/button.module.scss
index ab59315..5b3fa1f 100644
--- a/src/styles/btns/button.module.scss
+++ b/src/styles/btns/button.module.scss
@@ -6,6 +6,7 @@
   justify-content: center;
   align-self: flex-start;
   flex-grow: 0;
+  gap: 0.4rem;
   min-height: 2.6rem;
   padding: 0.65rem 1.4rem;
   border: 1px solid transparent;
@@ -138,4 +139,14 @@
     color: variable.$color-black;
     background: variable.$color-text;
   }
+}
+
+.icon {
+  display: inline-flex;
+  flex-shrink: 0;
+
+  svg,
+  path {
+    fill: currentColor;
+  }
 }
\ No newline at end of file
diff --git a/src/styles/btns/editButton.module.scss b/src/styles/btns/editButton.module.scss
deleted file mode 100644
index 853ee3e..0000000
--- a/src/styles/btns/editButton.module.scss
+++ /dev/null
@@ -1,80 +0,0 @@
-@use "../../variables" as variable;
-
-.btn {
-  border-radius: 7px;
-  font-weight: 600;
-  cursor: pointer;
-  letter-spacing: 0.3px;
-  transition: all 0.5s ease-in-out;
-  display: inline-flex;
-  align-items: center;
-  gap: 7px;
-  flex-grow: 0;
-  padding: 5px 10px;
-  font-size: variable.$font-2xs;
-  background: none;
-
-  svg {
-    transition: all 0.5s ease-in-out;
-  }
-}
-
-.primary {
-  color: variable.$color-tennis;
-  border: 1px solid variable.$color-tennis;
-
-  .edit {
-    fill: variable.$color-tennis;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.secondary {
-  color: variable.$color-purple;
-  border: 1px solid variable.$color-purple;
-
-  .edit {
-    fill: variable.$color-purple;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.light {
-  color: variable.$color-text;
-  border: 1px solid variable.$color-text;
-
-  .edit {
-    fill: variable.$color-text;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: #ddcefd;
-    border: 1px solid #ddcefd;
-
-    svg {
-      fill: #ddcefd;
-    }
-  }
-}
diff --git a/src/styles/btns/iconButton.module.scss b/src/styles/btns/iconButton.module.scss
deleted file mode 100644
index 01696bc..0000000
--- a/src/styles/btns/iconButton.module.scss
+++ /dev/null
@@ -1,83 +0,0 @@
-@use "../../variables" as variable;
-
-.btn {
-  border-radius: 7px;
-  font-weight: 600;
-  cursor: pointer;
-  letter-spacing: 0.3px;
-  transition: all 0.5s ease-in-out;
-  display: inline-flex;
-  align-items: center;
-  gap: 7px;
-  flex-grow: 0;
-  align-self: flex-start;
-  font-size: variable.$font-xs;
-  padding: 5px 10px;
-  background: none;
-
-  svg {
-    transition: all 0.5s ease-in-out;
-  }
-}
-
-.primary {
-  background-color: variable.$color-black;
-  color: variable.$color-tennis;
-  border: 1px solid variable.$color-tennis;
-
-  .icon {
-    fill: variable.$color-tennis;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.secondary {
-  background-color: variable.$color-black;
-  color: variable.$color-purple;
-  border: 1px solid variable.$color-purple;
-
-  .icon {
-    fill: variable.$color-purple;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.light {
-  color: variable.$color-text;
-  border: 1px solid variable.$color-text;
-
-  .icon {
-    fill: variable.$color-text;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: #ddcefd;
-    border: 1px solid #ddcefd;
-
-    svg {
-      fill: #ddcefd;
-    }
-  }
-}
diff --git a/src/styles/btns/plusButton.module.scss b/src/styles/btns/plusButton.module.scss
deleted file mode 100644
index afac6ef..0000000
--- a/src/styles/btns/plusButton.module.scss
+++ /dev/null
@@ -1,93 +0,0 @@
-@use "../../variables" as variable;
-
-.btn {
-  font-size: variable.$font-sm;
-  border-radius: 7px;
-  padding: 7px 13px;
-  font-weight: 600;
-  cursor: pointer;
-  transition: all 0.5s ease-in-out;
-  display: inline-flex;
-  align-items: center;
-  gap: 7px;
-  flex-grow: 0;
-  align-self: flex-start;
-  background: none;
-
-  svg {
-    transition: all 0.5s ease-in-out;
-  }
-}
-
-.primary {
-  color: variable.$color-tennis;
-  border: 1px solid variable.$color-tennis;
-
-  svg {
-    fill: variable.$color-tennis;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.secondary {
-  color: variable.$color-purple;
-  border: 1px solid variable.$color-purple;
-
-  svg {
-    fill: variable.$color-purple;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-
-    svg {
-      fill: variable.$color-text;
-    }
-  }
-}
-
-.light {
-  color: variable.$color-text;
-  border: 1px solid variable.$color-text;
-
-  svg {
-    fill: variable.$color-text;
-    height: 0.8em;
-    width: auto;
-  }
-
-  &:hover {
-    color: #ddcefd;
-    border: 1px solid #ddcefd;
-
-    svg {
-      fill: #ddcefd;
-    }
-  }
-}
-
-@media only screen and (-webkit-min-device-pixel-ratio: 2),
-  only screen and (min--moz-device-pixel-ratio: 2),
-  only screen and (-o-min-device-pixel-ratio: 2/1),
-  only screen and (min-device-pixel-ratio: 2),
-  only screen and (min-resolution: 192dpi),
-  only screen and (min-resolution: 2dppx) {
-  .btn {
-    font-size: variable.$font-xs;
-    padding: 5px 10px;
-    font-weight: 600;
-  }
-}
diff --git a/src/styles/btns/requestLinkButton.module.scss b/src/styles/btns/requestLinkButton.module.scss
deleted file mode 100644
index 6540a87..0000000
--- a/src/styles/btns/requestLinkButton.module.scss
+++ /dev/null
@@ -1,120 +0,0 @@
-@use "../../variables" as variable;
-
-.newLinkBtn {
-  width: 100%;
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  text-align: center;
-  gap: 1rem;
-  padding: 0.625rem 1.5rem; // was 10px 25px;
-  margin: 0 1.3rem;
-  background: none;
-  color: variable.$color-purple;
-  border: 2px solid variable.$color-purple;
-  border-radius: 6.25rem;
-  cursor: pointer;
-  transition: background-color 0.3s ease, color 0.3s ease;
-  font-size: variable.$font-sm;
-  font-weight: 500;
-  letter-spacing: variable.$spacing;
-
-  .circle {
-    width: 19px;
-    height: 19px;
-    display: flex;
-    align-items: center;
-    justify-content: center;
-    border: 2px solid variable.$color-purple;
-    background-color: variable.$color-purple;
-    border-radius: 50%;
-    position: relative;
-    transition: border-color 0.3s ease;
-
-    .icon {
-      position: absolute;
-      font-size: variable.$font-xs;
-      opacity: 0;
-      transition: opacity 0.3s ease;
-      font-weight: bold;
-    }
-
-    .arrow {
-      opacity: 1;
-
-      color: variable.$color-black;
-    }
-
-    .check {
-      opacity: 0;
-      color: variable.$color-black;
-    }
-  }
-
-  .text {
-    transition: color 0.3s ease;
-  }
-
-  &:hover:not(.loading):not(.activated) {
-    .circle .arrow {
-      opacity: 0;
-      transform: scale(0.8) rotate(45deg);
-    }
-
-    .circle .check {
-      opacity: 1;
-      transform: scale(1) rotate(0deg);
-    }
-  }
-
-  &.loading {
-    border: 1px solid variable.$color-tennis;
-
-    .circle {
-      border: 2px solid transparent;
-      border-top: 2px solid variable.$color-tennis;
-      animation: spin 1s linear infinite;
-      background-color: transparent;
-
-      .icon {
-        opacity: 0;
-      }
-    }
-
-    .text {
-      color: variable.$color-tennis;
-    }
-  }
-
-  &.activated {
-    background-color: variable.$color-black;
-    color: variable.$color-tennis;
-    border: 1px solid variable.$color-tennis;
-
-    .circle {
-      border-color: variable.$color-tennis;
-      background-color: variable.$color-tennis;
-
-      .arrow {
-        opacity: 0;
-      }
-
-      .check {
-        opacity: 1;
-      }
-    }
-
-    .text {
-      color: variable.$color-tennis;
-    }
-  }
-}
-
-@keyframes spin {
-  0% {
-    transform: rotate(0deg);
-  }
-  100% {
-    transform: rotate(360deg);
-  }
-}
diff --git a/src/styles/btns/squareButton.module.scss b/src/styles/btns/squareButton.module.scss
deleted file mode 100644
index 3b15c84..0000000
--- a/src/styles/btns/squareButton.module.scss
+++ /dev/null
@@ -1,56 +0,0 @@
-@use "../../variables" as variable;
-
-.btn {
-  font-size: variable.$font-xs;
-  font-weight: 600;
-  letter-spacing: 0.5px;
-  border-radius: 7px;
-  cursor: pointer;
-  transition: all 0.5s ease-in-out;
-  display: inline-flex;
-  align-items: center;
-  flex-grow: 0;
-  align-self: flex-start;
-  padding: 5px 15px;
-  background: none;
-}
-
-.primary {
-  color: variable.$color-tennis;
-  border: 1px solid variable.$color-tennis;
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-  }
-}
-
-.secondary {
-  color: variable.$color-purple;
-  border: 1px solid variable.$color-purple;
-
-  &:hover {
-    color: variable.$color-text;
-    border: 1px solid variable.$color-text;
-  }
-}
-
-.cancel {
-  color: variable.$color-fire;
-  border: 1px solid variable.$color-fire;
-
-  &:hover {
-    border: 1px solid #f44662;
-    color: #f44662;
-  }
-}
-
-.light {
-  color: variable.$color-text;
-  border: 1px solid variable.$color-text;
-
-  &:hover {
-    color: #ddcefd;
-    border: 1px solid #ddcefd;
-  }
-}
diff --git a/src/styles/components/accounts/row.module.scss b/src/styles/components/accounts/row.module.scss
index d30d702..b57e52f 100644
--- a/src/styles/components/accounts/row.module.scss
+++ b/src/styles/components/accounts/row.module.scss
@@ -59,7 +59,7 @@
   }
 }
 
-@media (max-width: 620px) {
+@include variable.respond(mobile-lg) {
   .rowContainer {
     padding: 0.75rem;
   }
diff --git a/src/styles/dashboard/dashboard.module.scss b/src/styles/dashboard/dashboard.module.scss
index cd924f2..fe1c001 100644
--- a/src/styles/dashboard/dashboard.module.scss
+++ b/src/styles/dashboard/dashboard.module.scss
@@ -45,13 +45,13 @@
   gap: 0.65rem;
 }
 
-@media (max-width: 980px) {
+@include variable.respond(tablet-lg) {
   .dashboardContainer {
     grid-template-columns: 1fr;
   }
 }
 
-@media (max-width: 560px) {
+@include variable.respond(mobile-lg) {
   .dashboardContainer {
     gap: 1rem;
     padding-top: 0;
diff --git a/src/styles/elements/languageSelector.module.scss b/src/styles/elements/languageSelector.module.scss
index 1dacdcc..c58c62f 100644
--- a/src/styles/elements/languageSelector.module.scss
+++ b/src/styles/elements/languageSelector.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 
 .languageSelectorContainer {
diff --git a/src/styles/inputs/formInput.module.scss b/src/styles/inputs/formInput.module.scss
index 7a4298f..ab090f0 100644
--- a/src/styles/inputs/formInput.module.scss
+++ b/src/styles/inputs/formInput.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .formInput {
   display: flex;
diff --git a/src/styles/inputs/formTextArea.module.scss b/src/styles/inputs/formTextArea.module.scss
index e498498..ffd91e9 100644
--- a/src/styles/inputs/formTextArea.module.scss
+++ b/src/styles/inputs/formTextArea.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .formTextArea {
   display: flex;
diff --git a/src/styles/inputs/searchInput.module.scss b/src/styles/inputs/searchInput.module.scss
index 2cabfbe..cee544a 100644
--- a/src/styles/inputs/searchInput.module.scss
+++ b/src/styles/inputs/searchInput.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .formInput {
   display: flex;
diff --git a/src/styles/interface/messagComponent.module.scss b/src/styles/interface/messagComponent.module.scss
index 04ae7be..8c33375 100644
--- a/src/styles/interface/messagComponent.module.scss
+++ b/src/styles/interface/messagComponent.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .messageContainer {
   width: 40px;
diff --git a/src/styles/layout/layout.module.scss b/src/styles/layout/layout.module.scss
index 34a9f8a..40aee86 100644
--- a/src/styles/layout/layout.module.scss
+++ b/src/styles/layout/layout.module.scss
@@ -52,14 +52,14 @@
   padding-top: 0.25rem;
 }
 
-@media (max-width: 768px) {
+@include variable.respond(tablet) {
   .layout {
     gap: 0.75rem;
     padding: 0.75rem;
   }
 
   .sidebar {
-    min-height: calc(100dvh - 1.5rem);
+    min-height: 0;
   }
 
   .bodyLayout {
diff --git a/src/styles/layout/navbar.module.scss b/src/styles/layout/navbar.module.scss
index 3212508..a413009 100644
--- a/src/styles/layout/navbar.module.scss
+++ b/src/styles/layout/navbar.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .navbarContainer {
   display: flex;
@@ -62,7 +62,43 @@
   min-width: 0;
 }
 
-@media (max-width: 520px) {
+// زر فتح القائمة (hamburger) — يبان بس على الموبايل
+.menuButton {
+  display: none;
+  align-items: center;
+  justify-content: center;
+  flex-shrink: 0;
+  width: 2.5rem;
+  height: 2.5rem;
+  color: variable.$color-text;
+  background: transparent;
+  border: 1px solid variable.$color-border;
+  border-radius: variable.$radius-xs;
+  cursor: pointer;
+  transition: background 0.2s ease, border-color 0.2s ease;
+
+  svg {
+    fill: currentColor;
+  }
+
+  &:hover {
+    background: variable.$surface-3;
+    border-color: variable.$color-border-strong;
+  }
+
+  &:focus-visible {
+    outline: 2px solid variable.$color-tennis;
+    outline-offset: 2px;
+  }
+}
+
+@include variable.respond(tablet) {
+  .menuButton {
+    display: flex;
+  }
+}
+
+@include variable.respond(mobile) {
   .navbarContainer {
     padding: 0.65rem 0.75rem;
   }
diff --git a/src/styles/layout/sidebar/sidebar.module.scss b/src/styles/layout/sidebar/sidebar.module.scss
index 4af7a9d..2c192ca 100644
--- a/src/styles/layout/sidebar/sidebar.module.scss
+++ b/src/styles/layout/sidebar/sidebar.module.scss
@@ -136,8 +136,87 @@
   }
 }
 
-@media (max-width: 768px) {
+// زر سكر الـ drawer — يبان بس على الموبايل (شوف respond(tablet) تحت)
+.mobileClose {
+  display: none;
+  align-items: center;
+  justify-content: center;
+  align-self: flex-end;
+  flex-shrink: 0;
+  width: 2rem;
+  height: 2rem;
+  margin-bottom: 0.5rem;
+  color: variable.$color-text-gray;
+  background: transparent;
+  border: none;
+  border-radius: 50%;
+  cursor: pointer;
+  transition: color 0.2s ease, background 0.2s ease;
+
+  svg {
+    fill: currentColor;
+  }
+
+  &:hover {
+    color: variable.$color-text;
+    background: variable.$surface-3;
+  }
+}
+
+// الخلفية الشفافة (backdrop) خلف الـ drawer — غير موجودة بصرياً إطلاقاً
+// على الديسكتوب، وبتفعّل بس تحت breakpoint الموبايل.
+.backdrop {
+  display: none;
+}
+
+@include variable.respond(tablet) {
   .sidebarContainer.open {
     width: 13rem;
   }
+
+  // تحويل الـ sidebar لـ drawer عائم (overlay) بدل ما ياخد مساحة بالـ layout
+  .sidebarContainer {
+    position: fixed;
+    top: 0;
+    left: 0;
+    z-index: 1100;
+    width: min(82vw, 300px) !important;
+    height: 100dvh;
+    min-height: 0;
+    margin: 0;
+    border-radius: 0;
+    transform: translateX(-100%);
+    transition: transform 0.3s ease;
+
+    // على الموبايل الـ drawer دايماً كامل (isOpen بيصير showFullContent
+    // من الكومبوننت)، فمنخفي زر الطي تبع الديسكتوب هون
+    .btn {
+      display: none;
+    }
+  }
+
+  .sidebarContainer.mobileOpen {
+    transform: translateX(0);
+    box-shadow: variable.$shadow-card;
+  }
+
+  .mobileClose {
+    display: flex;
+  }
+
+  .backdrop {
+    display: block;
+    position: fixed;
+    inset: 0;
+    z-index: 1090;
+    background: rgba(2, 6, 23, 0.6);
+    opacity: 0;
+    pointer-events: none;
+    transition: opacity 0.25s ease;
+
+    &.visible {
+      opacity: 1;
+      pointer-events: auto;
+    }
+  }
 }
\ No newline at end of file
diff --git a/src/styles/rapport/rapport.module.scss b/src/styles/rapport/rapport.module.scss
index 6968dcb..46acc43 100644
--- a/src/styles/rapport/rapport.module.scss
+++ b/src/styles/rapport/rapport.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 .header{
   display: flex;
   justify-content: flex-start;
diff --git a/src/styles/rapport/row.module.scss b/src/styles/rapport/row.module.scss
index d5e838a..f806c01 100644
--- a/src/styles/rapport/row.module.scss
+++ b/src/styles/rapport/row.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 
 .rowContainer {
   display: flex;
diff --git a/src/styles/sidebar/sidebar.module.scss b/src/styles/sidebar/sidebar.module.scss
index 1001e25..256baf5 100644
--- a/src/styles/sidebar/sidebar.module.scss
+++ b/src/styles/sidebar/sidebar.module.scss
@@ -1,4 +1,4 @@
-@use "../../variables" as variable;
+@use "../variables" as variable;
 @import url("https://fonts.googleapis.com/css2?family=Agdasima:wght@400;700&display=swap");
 
 .sidebarContainer {
